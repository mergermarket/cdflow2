version: 2
team: test-team
config:
    image: test-config-image
    params:
        config-key: config-value
builds:
    release:
        image: test-release-image
terraform:
    image: test-terraform-image
